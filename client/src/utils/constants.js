export const ROLES = Object.freeze({
  ADMIN: 'ADMIN',
  WAREHOUSE_STAFF: 'WAREHOUSE_STAFF',
});

export const TOKEN_KEY = 'warehouse_token';
export const THEME_KEY = 'warehouse_theme';

export const DEFAULT_PAGE = 1;
export const DEFAULT_LIMIT = 20;
